# 実装と出典の方針

本プロジェクトは、実装の由来を確認できる独立開発のMIT Licenseエミュレーター／SDKを目指します。

## 参照情報の分類

1. **一次資料**：所有者による実機測定、基板写真、IC刻印、所有者が取得したROMの照合値、オシロスコープ／ロジックアナライザー結果、部品メーカーのデータシート、取扱説明書
2. **二次資料**：公開サイトにあるJR-800の挙動説明
3. **参照エミュレーター**：調査項目や外部挙動比較には使えるが、ライセンスが非互換な場合は実装元にできないソフトウェア

## 既知の参照実装

- PockEmulはJR-800に対応するGPLv2ソフトウェアであり、実装をコピー、翻訳、移植、構造的に再現しません。
- Common Source Code ProjectのeJR-800はPockEmul由来と記載されているため、独立した裏付けとは扱わず、実装をコピー、翻訳、移植しません。
- MAMEには6801系CPUとHD44102に関係するBSD-3-Clause実装がありますが、本プロジェクトは原則として独立実装し、直接再利用する場合は事前に設計とライセンスを決定してBSD表示を保持します。

## 独立実装

開発者は、事実仕様と一次資料を利用できます。
非互換ライセンスのソースを変換して本プロジェクトのコードにしてはいけません。
非互換実装で初めて気付いた挙動は仮説として扱い、実機、公開資料、外部から観察できるblack-box testのいずれかで独立に確認してから実装へ使います。

未確認の実機挙動は既定値にせず、明示的な実験入力または未対応エラーとして扱います。
ROM、私有資料、実機記録は公開ソースへ含めません。
