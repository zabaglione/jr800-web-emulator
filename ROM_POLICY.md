# ROMの取り扱い方針

このリポジトリは、National JR-800 BASIC ROMを含む市販ファームウェアを配布しません。

## 守ること

- 拡張子にかかわらず、ROMイメージとその断片をGitへ追加しないでください。
- ROMから取り出したバイト列、ROM由来ソース／逆アセンブル、ROMを含むテストデータ、ROM dumpのWAVも追加しないでください。
- ビルドと自動テストは市販ROMなしで動作させます。
- エミュレーターのテストには、プロジェクト内で作成した合成プログラムと合成ROMだけを使います。
- 利用者は、所有する実機から取得したROMをローカルで読み込めます。
- Web UIが読み込んだROMはブラウザー内のWorkerメモリーだけで処理し、サーバーへ送信しません。
- 実機録音のWAVは、ローカルで`jr8wav`を使って変換します。

## ROM取得

実機からの取得には、取扱説明書に記載されたBASICの`MSAVE`を使います。
1回の`MSAVE`を1個のWAVへ録音し、`jr8wav decode-native-msave`で開始アドレス付きの`.j8r`へ変換します。
録音先頭の無音はデータや録音回数を表すものではありません。

必要なら、別々に録音した2個のWAVを`verify-native-msave`で比較できます。
これは任意の再現性検査であり、通常の変換に2録音を必須とはしません。

## ROM形式

推奨形式はJR8ROMバージョン1、拡張子は`.j8r`です。
各セグメントの論理開始アドレス、バイト範囲、境界を保持し、物理ROMの個数や配線は推測しません。
離れた範囲を持てますが、空きを埋めたり隣接セグメントを結合したりしません。
仕様は[JR8ROMアドレス付きROMコンテナー1.0](docs/formats/jr8rom-v1.md)を参照してください。

Web UIは、以前から使われている32 KiBのRAWデータも`.rom`として読み込めます。
`.rom`は`$8000-$FFFF`へ固定配置し、アドレス情報と完全性検査を持ちません。
選択時には`.j8r`への変換案内を表示し、利用者が続行を確認した場合だけ読み込みます。
コマンドラインの`jr8run jr800`は`.j8r`だけを受け付けます。

独立して作成したJR8ROMを結合する場合は、全入力の完全性を検証し、範囲重複がないことを要求します。
`jr8rom inspect`は形式、レコード番号、論理範囲、長さだけを表示し、ROMバイト、ダイジェスト、ローカルパスを表示しません。
